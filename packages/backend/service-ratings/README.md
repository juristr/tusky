# service-ratings

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build service-ratings` to build the library.

## Running unit tests

Run `nx test service-ratings` to execute the unit tests via [Vitest](https://vitest.dev/).
